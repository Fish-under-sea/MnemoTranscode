# 子项目 E · 后端工程化 + 媒体服务 · 收尾记录

**完成日期**：2026-04-24  
**主导分支**：`Codex-coding`  
**执行模型**：GPT-5.3 Codex（规格定调：Opus）

---

## 一句话总结

E 子项目完成了后端工程化底座（Alembic、统一错误响应、request_id、结构化日志基础）、members 语义迁移、媒体两阶段接口骨架与 TTS/Celery 占位能力，为 B/C/D 子项目提供了稳定后端契约。

---

## 执行时间线（milestone）

| Milestone | Commit | 模型 | 说明 |
|---|---|---|---|
| M2a | `4a1bdbf` | Codex | Alembic 骨架、baseline、退役 create_all |
| M2b | `329b563` | Codex | members `status/end_year` 迁移 + ORM 同步 |
| M1 | `8d18436` | Codex | Pydantic 归一化、冲突检测、兼容策略 |
| M3 | `51e293a` | Codex | 统一错误 envelope + request_id 中间件 |
| M4 | `34d664a` | Codex | archive/memory/media 路由迁移到领域异常 |
| M5 | `1ff9732` | Codex | 两阶段上传路由 + media 模型/迁移骨架 |
| M6 | `95b9582` | Codex | VoiceProvider 抽象 + TTS 服务 + Celery 占位 |
| M7 | pending | Codex | compose/healthz/文档收尾 |

---

## 关键产出

- `backend/alembic/` 与 `backend/alembic.ini`
- `backend/alembic/versions/861ffc1e3691_baseline_existing_schema.py`
- `backend/alembic/versions/b5b55762bdc8_members_status_end_year.py`
- `backend/alembic/versions/6ef86496200c_media_tables.py`
- `backend/app/core/exceptions.py`
- `backend/app/api/middleware/request_id.py`
- `backend/app/api/middleware/exception_handlers.py`
- `backend/app/models/media.py`
- `backend/app/services/voice/*`
- `backend/app/workers/*`

---

## 测试与验证

- `pytest backend/tests/integration -v`：当前新增 14 条集成测试全部通过。
- 迁移链路在“本机 PostgreSQL 不可连接”场景下无法完成在线验证，已保留在 backlog（服务恢复后需补跑 `alembic upgrade head` 端到端验证）。

---

## 遗留与 backlog

- M5 的完整对象存储 E2E（init -> PUT -> complete -> GET）仍需在可用 MinIO/Postgres 环境补测。
- M6 的 provider 超时重试策略已落地骨架，但缺少真实 provider 的失败注入测试（当前以服务级单测为主）。
- 前端 `types` 路径在当前仓库未发现标准目录，需要 B/C 子项目对调用路径切换后再做最终适配 PR。
- 若无 CI 流水线，建议后续增加最小 CI（pytest + alembic check）。

